// phla javaScript me modules and classes nahi hutii thi 
"use strict"; // treat all js code as a newer js version but there is no need to write a 
// code in strict mode because js code is already in strict mode.likewise its a code syntax to write a code in a newer syantax.
//alert(3+3) // its give an error on node but its easily run on browser because inside abrowser their dom of js that enable the js syntax on browser i.e console.
// In node there is a synatx to run the "alert".
// most of the instructor use alert for arithematic operation 


let name="bisma" // string data type.
let age= 18 // number value =>  the range of the number value is 2 to the power of 53 its a range of the memory address
let loggedIn = true // boolean type
// bigint is not used but a large website i.e facebook stock market use big int .

//string=> is used in double qutation or a single qutation 
// bolean=> true/false
// null=> is a data type or a stand alone value and its a represtation of a empty value. i.e let state=null.
// Null example => we want to check temperature k teempareture show na hu toh tmhain voh 0 output deta h toh mtlb temperature show hu rha h mgr hmain temperature show na hu toh voh temperature:null likhain ga .
//undefine=> mtlb yh k value define nahi ki only variable declare kr diya h i.e let accountState;
// symbol => to check the uniqueness in the component 
//  {string , bolean,null, undefine,symbol is a premative data types}


// Null datatype is an Object

//object

//console.log(typeof())